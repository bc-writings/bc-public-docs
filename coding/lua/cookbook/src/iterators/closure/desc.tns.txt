this::
    date = 2025-09-14


====================================
XXX
====================================

YYY


Explication courte

iter2D(imax, jmax) construit et renvoie une fonction _iter2D.

À chaque appel par la boucle for, _iter2D incrémente les indices (i, j).

Tant que i ≤ imax, il renvoie (i, j).

Quand i dépasse, il revient à i=1 et passe à la ligne suivante (j=j+1).

La boucle s’arrête quand j > jmax.

👉 Résultat : la boucle for a,b in iter2D(3,5) parcourt toutes les coordonnées d’une grille 2D de largeur imax et hauteur jmax.
