%%%
% Le travail est fait par les fonctions ''Anp'', ''Cnp'' et aussi
% ''writefloat2int'' présentes dans le fichier ''main.lua''.
%%%

% Importation du fichier ''main.lua''.
\directlua{dofile('main.lua')}

% Les macros ''\Anp'' et ''\Cnp'' utilisent la fonction ''writefloat2int''
% pour imprimer des valeurs flottantes sans partie décimale.
\NewDocumentCommand{\Anp}{mm}{%
  \directlua{writefloat2int(Anp(#1, #2))}%
}

\NewDocumentCommand{\Cnp}{mm}{%
  \directlua{writefloat2int(Cnp(#1, #2))}%
}
