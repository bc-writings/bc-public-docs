%%%
% Le travail est fait par les fonctions ''Anp'', ''Cnp'' et aussi
% ''writefloat2int'' présentes dans le fichier ''main.lua''.
%
% note::
%     Pour imprimer des valeurs flottantes renvoyés par \lua sans
%     partie décimale, les macros ''\Anp'' et ''\Cnp''ont besoin
%     de la fonction ''writefloat2int''.
%%%

%%%
% Importation du fichier ''main.lua'' contenant les deux fonctions
% ''Anp'' et ''Cnp'', ainsi que la fonction ''writefloat2int''.
%%%
\directlua{dofile('main.lua')}

%%%
% prototype::
%     #1 : le "naturel" n du nombre d'arrangements de p parmi n.
%     #2 : le "naturel" p du nombre d'arrangements de p parmi n.
%%%
\NewDocumentCommand{ \Anp }{ m m }{%
  \directlua{writefloat2int(Anp(#1, #2))}%
}

%%%
% prototype::
%     #1 : le "naturel" n du nombre de combinaisons de p parmi n.
%     #2 : le "naturel" p du nombre de combinaisons de p parmi n.
%%%
\NewDocumentCommand{ \Cnp }{ m m }{%
  \directlua{writefloat2int(Cnp(#1, #2))}%
}
