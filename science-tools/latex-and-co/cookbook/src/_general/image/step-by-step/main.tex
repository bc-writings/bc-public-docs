\documentclass[varwidth, border = 3pt]{standalone}

\usepackage{main}

%%%
% Le package ''mwe'' nous fournit des images factices.
%%%
\usepackage{mwe}

\begin{document}

Voici un exemple d'images affichées pas à pas...

\stepbystep{example-image-}{a,a,b,b,c,c}


\medskip


Sur 3 colonnes, un fond coloré, et sans cadre ? Aucun problème, c'est facile à obtenir...

\stepbystep[
  raster columns = 3,
  colback        = orange!25,
  boxrule        = 0pt,
  top            = 2pt,
  bottom         = 2pt,
  left           = 2pt,
  right          = 2pt,
]{example-image-}{a,a,a,b,b,b}

Facile ! Non ?

\end{document}
