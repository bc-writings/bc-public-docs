% Source: https://tex.stackexchange.com/a/192000/6880

\documentclass{scrartcl}

\ExplSyntaxOn

\NewDocumentCommand \RULE {} {
  \rule { \l_tmpa_dim } { \l_tmpb_dim }
}

\keys_define:nn { foo } {
  bar .code:n = { \keys_set:nn { foo / bar } { #1 } },
  bar / tmpa .dim_set:N = \l_tmpa_dim,
  bar / tmpa .initial:n = 1em,
}

\keys_define:nn { foo } {
  bar / tmpb .dim_set:N = \l_tmpb_dim,
  bar / tmpb .initial:n = 4em,
}

\NewDocumentCommand \dimsetup { m } { 
  \keys_set:nn { foo } { #1 } 
}

\ExplSyntaxOff

\dimsetup{
  bar = {
    tmpa = 7cm,
    tmpb = 3pt
  }
}


\begin{document}

\RULE

\end{document}