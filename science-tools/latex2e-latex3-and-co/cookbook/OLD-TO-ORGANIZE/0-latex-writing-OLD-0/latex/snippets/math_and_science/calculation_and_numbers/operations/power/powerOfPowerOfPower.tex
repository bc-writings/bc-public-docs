% Source : http://tex.stackexchange.com/questions/65078/is-there-a-more-correct-way-to-show-explicit-exponential-operators

\documentclass[10pt,a4paper]{article}
	\usepackage[utf8x]{inputenc}
	\usepackage{ucs}
	\usepackage{amsmath}
	\usepackage{amsfonts}
	\usepackage{amssymb}

	\newcommand\recexpo{\mathbin{\uparrow}}


\begin{document}

$
	b^{\alpha \recexpo n}
	=
	\bigl(
		\cdots
		\bigl(
			\bigl(
				b \underbrace{^\alpha
			\bigr)
			^\alpha
		\bigr)
		^{\cdots}
	\bigr)
	^\alpha}_\text{$n$ times}
	= b^{(\alpha^n)}
$

Plus ambigü :
$
	b \underbrace{
	  {{{^{\alpha\vphantom{h}}}^{\alpha\vphantom{h}}}^{\cdots\vphantom{h}}}^{\alpha\vphantom{h}}
	}_{\text{$n$ times}}
	= b^{(\alpha^n)}
$

\end{document}