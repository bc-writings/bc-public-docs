% Sources :
%    1) http://tex.stackexchange.com/questions/38561/hiding-a-counter-if-it-is-only-displayed-only-one-time
%    2) https://groups.google.com/group/fr.comp.text.tex/browse_thread/thread/e54e78671fc6e823/e2ae3eaee83349a8?hl=fr#e2ae3eaee83349a8

\documentclass{article}
	\makeatletter
		\newcommand{\defineoneshotcounter}[1]{%
			\newcounter{#1}
			\newcounter{#1@helper}
			\expandafter\newcommand\csname show#1\endcsname[1][\arabic]{%
				\stepcounter{#1}%
				\stepcounter{#1@helper}%
				\@ifundefined{#1@reset@\the\csname c@#1@helper\endcsname}{##1{#1}}{}%
			}
			\@namedef{reset#1}{%
				\ifnum\csname c@#1\endcsname=1\if@filesw
				\immediate\write\@auxout{%
					\string\global\string\@namedef{#1@reset@\the\csname
					c@#1@helper\endcsname}{}%
				}%
				\fi\fi
				\setcounter{#1}{0}%
			}
			\AtEndDocument{\@nameuse{reset#1}} % SIC
		}
	\makeatother

	\defineoneshotcounter{FirstCounter}
	\newcommand{\FirstCounterStyle}[1]{\{\alph{#1}\}}

	\defineoneshotcounter{SecundCounter}
	\newcommand{\SecundCounterStyle}[1]{\{\alph{#1}\}} % Problème ?

	\setlength{\parindent}{0cm}


\begin{document}

\section{First counter}

TEST : \showFirstCounter,
TEST : \showFirstCounter,
TEST : \showFirstCounter,
RESET : \resetFirstCounter

TEST : \showFirstCounter[\alph],
TEST : \showFirstCounter[\alph],
RESET : \resetFirstCounter

TEST : \showFirstCounter,
RESET : \resetFirstCounter

TEST : \showFirstCounter[\FirstCounterStyle],
TEST : \showFirstCounter[\FirstCounterStyle],
TEST : \showFirstCounter[\FirstCounterStyle],
TEST : \showFirstCounter[\FirstCounterStyle],
RESET : \resetFirstCounter

TEST : \showFirstCounter,


\section{Secund counter}

TEST : \showSecundCounter[\SecundCounterStyle],
TEST : \showSecundCounter[\SecundCounterStyle],
TEST : \showSecundCounter[\SecundCounterStyle],
RESET : \resetSecundCounter

TEST : \showSecundCounter[\arabic],
TEST : \showSecundCounter[\arabic],
RESET : \resetSecundCounter

TEST : \showSecundCounter,
RESET : \resetSecundCounter

TEST : \showSecundCounter,

\end{document}